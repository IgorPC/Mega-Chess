export const ADMIN_PROFILE_CONTROLLER_PATH = 'admin/me';

export const ADMIN_PROFILE_ROUTES = {
  PASSWORD: 'password',
} as const;

/** Minimum password length enforced on change-password. */
export const ADMIN_PROFILE_MIN_PASSWORD_LENGTH = 12;
